import Home from '@/components/home';

/**
 * HomePage Component
 * Simple wrapper component that renders the main Home component
 * Used for routing purposes
 */
const HomePage = () => {
  return <Home />;
};

export default HomePage;
